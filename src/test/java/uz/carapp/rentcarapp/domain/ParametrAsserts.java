package uz.carapp.rentcarapp.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ParametrAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertParametrAllPropertiesEquals(Parametr expected, Parametr actual) {
        assertParametrAutoGeneratedPropertiesEquals(expected, actual);
        assertParametrAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertParametrAllUpdatablePropertiesEquals(Parametr expected, Parametr actual) {
        assertParametrUpdatableFieldsEquals(expected, actual);
        assertParametrUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertParametrAutoGeneratedPropertiesEquals(Parametr expected, Parametr actual) {
        assertThat(actual)
            .as("Verify Parametr auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertParametrUpdatableFieldsEquals(Parametr expected, Parametr actual) {
        assertThat(actual)
            .as("Verify Parametr relevant properties")
            .satisfies(a -> assertThat(a.getName()).as("check name").isEqualTo(expected.getName()))
            .satisfies(a -> assertThat(a.getStatus()).as("check status").isEqualTo(expected.getStatus()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertParametrUpdatableRelationshipsEquals(Parametr expected, Parametr actual) {
        // empty method
    }
}
